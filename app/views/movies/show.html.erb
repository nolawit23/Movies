<p style="color: green"><%= notice %></p>

<%= render @movie %>

<div class="mb-4 mt-3">
 <%= link_to "Edit", edit_movie_path(@movie), class: "btn btn-success mr-2" %> |
  <%= link_to "Back to movies", movies_path, class: "btn btn-secondary" %>
  <br>

  <%= link_to "Delete", @movie, method: :delete, data: { confirm: "Are you sure?" }, class: "btn btn-danger mt-3" %>
</div>
